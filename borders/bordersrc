#!/bin/bash
# JankyBorders config — pairs with AeroSpace tiling WM
# Colors matched to sketchybar dark theme

options=(
    style=round
    width=4.0
    hidpi=on
    active_color=0xffaac8ff
    inactive_color=0x30aac8ff
)

borders "${options[@]}"
